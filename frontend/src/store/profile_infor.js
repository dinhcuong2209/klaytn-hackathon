export const state = () => ({
    infor: [],

});

export const mutations = {
    // set(state, locale) {
    //     const localeExists = _find(state.locales, { value: locale });
    //     if (localeExists) {
    //         state.locale = locale;
    //         this.$i18n.setLocale(locale);
    //     }
    // },
};
